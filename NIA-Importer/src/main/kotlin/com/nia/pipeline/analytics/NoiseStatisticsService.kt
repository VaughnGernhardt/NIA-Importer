package com.nia.pipeline.analytics

import com.nia.pipeline.analytics.model.NoisePoint

class NoiseStatisticsService {

    fun generate(): List<NoisePoint> {

        // Placeholder until we connect a real
        // crowdsourced noise dataset.

        return emptyList()
    }
}